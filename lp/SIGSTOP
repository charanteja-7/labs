
#include <stdio.h>
#include <stdlib.h>
#include <signal.h>
#include <unistd.h>

int main() {
    printf("Running... (Send SIGSTOP using `kill -STOP <pid>`)\n");
    while (1) {
        printf("Still running... (Press Ctrl+C to stop)\n");
        sleep(1);
    }
    return 0;
}
